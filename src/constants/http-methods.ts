/**
 * HTTP Request Methods Map
 */
export enum HttpMethods {
    // for support response all request
    ALL = 'all',

    // native http request methods
    HEAD = 'head',
    OPTIONS = 'options',
    GET = 'get',
    PUT = 'put',
    POST = 'post',
    PATCH = 'patch',
    DELETE = 'delete'
}
